User-agent: *
Allow: /
Disallow: /app$
Disallow: /app.html
Disallow: /admin
Disallow: /review

Sitemap: https://pracexam.com/sitemap.xml

# Notes (keep this file ASCII and keep rules at the top --
# some crawlers give up on long non-ASCII headers).
# /app$ uses an end anchor on purpose: a bare "/app" is a prefix
# match and would also block apple-touch-icon.png.
# Why /app and not just /app.html: Cloudflare Pages redirects
# .html to the extensionless URL, so /app is what visitors use.
# Full story: see HANDOFF.md (2026-08-31 entry) in the source repo.
